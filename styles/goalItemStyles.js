import { StyleSheet } from "react-native";

export default StyleSheet.create({
  goalItem: {
    marginVertical: 8,
    padding: 8,
    backgroundColor: "#5e0acc",
    color: "white",
    borderRadius: 10,
  },
});
