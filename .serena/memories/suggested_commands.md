# よく使うコマンド
- 依存関係インストール: `npm install`
- 開発サーバ起動: `npm start`（内部で `expo start`）
- Android 実機/エミュレータで起動: `npm run android`
- iOS シミュレータで起動: `npm run ios`
- Web プレビュー: `npm run web`

注: ネットワーク制限がある環境では初回の `npm install` がブロックされる可能性があるため要確認。